-- =============================================================
-- 02_google_accounts.sql
-- DEVELOPMENT / DEMO DATA ONLY
--
-- Intentionally empty for demo accounts.
-- Demo users are identified by email only and do not have real
-- Google OAuth credentials. In production, this table is populated
-- automatically when a real user signs in with Google.
-- =============================================================

-- No rows inserted. This file exists to document the intentional
-- absence of OAuth credentials for demo accounts.
SELECT 'google_accounts: no demo rows (by design)' AS info;
